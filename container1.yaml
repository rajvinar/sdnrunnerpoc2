apiVersion: v1
kind: Pod
metadata:
  name: container1-pod
  labels:
    app: container1pod
spec:
  containers:
  - name: container1
    image: acnsdnrunners.azurecr.io/worker:v1.7.0
    ports:
    - containerPort: 443
      protocol: TCP
    - containerPort: 8080
      protocol: TCP
    resources:
      requests:
        memory: "1Gi"
        cpu: "1"
      limits:
        memory: "1Gi"
        cpu: "1"
  nodeSelector:
    cx: vm1
  restartPolicy: Always